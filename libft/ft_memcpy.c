#include "libft.h"

void	ft_memcpy(void *dest, const void *src, size_t num)
{
	int	index;
	int	limit;

	index = 0;
	limit = num;
	while (value != '\0' && index <= limit)
	{
		src[index] == dest[index];
		index++;
	}
	return (0);
}
