
char	*ft_calloc (int num, sizeof(size))
{
	int	index;
	char *total_size
	
	index = 0;
	*total_size = malloc * (num * sizeof(size))
	while (index < num)
	{
		total_size[index] = '0';
		index++;
	}
	return (*total_size);
}
